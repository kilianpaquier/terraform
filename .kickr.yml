# Kickr configuration file (https://github.com/kickr-dev/kickr)
# yaml-language-server: $schema=https://raw.githubusercontent.com/kickr-dev/kickr/beta/.schemas/kickr.v1.schema.json
version: 1
ci:
  provider: gitlab
  options:
    - kickr
exclude:
  - shell
license: mit
maintainers:
  - name: u.kilianpaquier
platform: gitlab
terraform:
  apply: manual
  engine: opentofu
  environments:
    - production
  modules:
    - modules/cloud
    - modules/git
